<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Timerom.App.Views.Templates.Button.FloatActionButton">
  <ContentView.Content>
        <ImageButton Clicked="ImageButton_Clicked" VerticalOptions="End" HorizontalOptions="End" BackgroundColor="Transparent" Source="{AppThemeBinding Light=FloatActionButton_Lightmode.png, Dark=FloatActionButton_Darkmode.png}" WidthRequest="60" HeightRequest="60">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="1"></Setter>
                        </VisualState.Setters>
                    </VisualState>

                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.7"></Setter>
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </ImageButton>
    </ContentView.Content>
</ContentView>